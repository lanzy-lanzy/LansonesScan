package com.ml.lansonesscan

import android.app.Application

class LansonesScanApplication : Application() {
    override fun onCreate() {
        super.onCreate()
        // Dependency injection will be set up in later tasks
    }
}